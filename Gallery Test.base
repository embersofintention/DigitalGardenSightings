filters:
  and:
    - '!file.path.contains("_templates")'
    - file.tags.contains("art")
    - "!coverImage.isEmpty()"
views:
  - type: cards
    name: Art
    order:
      - file.name
      - tags
      - created_date
    image: note.coverImage
