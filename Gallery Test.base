filters:
  and:
    - '!file.path.contains("_templates")'
    - "!coverImage.isEmpty()"
views:
  - type: cards
    name: Art
    order:
      - file.name
      - tags
      - created_date
    image: note.coverImage
    imageFit: contain
